@name: "增强的默认值功能演示"
@description: "展示HTTP请求和断言关键字的新增默认值功能"

# 测试1：HTTP请求关键字的默认值
[打印], 内容: "=== 测试1：HTTP请求默认值功能 ==="

# 使用默认客户端（default）和默认禁用授权（false）
[打印], 内容: "模拟HTTP请求 - 使用默认客户端和默认设置"

# 测试2：断言关键字的默认值
[打印], 内容: "=== 测试2：断言关键字默认值功能 ==="

# 断言关键字使用默认消息
result1 = [生成随机数], 最小值: 50, 最大值: 100
[断言], 条件: "${result1} >= 50"  # 使用默认错误消息

# 手动指定消息
result2 = [生成随机数], 最小值: 1, 最大值: 10
[断言], 条件: "${result2} <= 10", 消息: "随机数应该在1-10范围内"

# 测试3：JSON断言的默认值
[打印], 内容: "=== 测试3：JSON断言默认值功能 ==="

test_json = '{"name": "张三", "age": 30, "city": "北京"}'

# 使用默认操作符（==）和默认消息
[JSON断言], JSON数据: ${test_json}, JSONPath: "$.name", 预期值: "张三"

# 使用自定义操作符，其他保持默认
[JSON断言], JSON数据: ${test_json}, JSONPath: "$.age", 预期值: 25, 操作符: ">"

# 测试4：类型断言的默认值
[打印], 内容: "=== 测试4：类型断言默认值功能 ==="

test_string = "hello world"
test_number = 42

# 使用默认错误消息
[类型断言], 值: ${test_string}, 类型: "string"
[类型断言], 值: ${test_number}, 类型: "number"

# 测试5：数据比较的默认值
[打印], 内容: "=== 测试5：数据比较默认值功能 ==="

value_a = 100
value_b = 100

# 使用默认操作符（==）和默认消息
[数据比较], 实际值: ${value_a}, 预期值: ${value_b}

# 使用自定义操作符
[数据比较], 实际值: ${value_a}, 预期值: 50, 操作符: ">"

# 测试6：综合测试 - 大量使用默认值
[打印], 内容: "=== 测试6：综合默认值测试 ==="

# 生成测试数据（使用各种默认值）
random_str = [生成随机字符串]  # 默认长度8，字母数字混合
random_num = [生成随机数]      # 默认0-100，整数
current_time = [获取当前时间]  # 默认timestamp，Asia/Shanghai时区

[打印], 内容: "生成的随机字符串: ${random_str}"
[打印], 内容: "生成的随机数: ${random_num}"
[打印], 内容: "当前时间戳: ${current_time}"

# 验证生成的数据（使用默认断言消息）
[断言], 条件: "len('${random_str}') == 8"
[断言], 条件: "${random_num} >= 0"
[断言], 条件: "${random_num} <= 100"
[断言], 条件: "${current_time} > 0"

# 字符串处理（使用默认操作strip）
test_text = "  hello world  "
cleaned_text = [字符串操作], 字符串: ${test_text}  # 默认strip操作
[打印], 内容: "清理后的文本: '${cleaned_text}'"

# 类型验证（使用默认消息）
[类型断言], 值: ${cleaned_text}, 类型: "string"
[类型断言], 值: ${current_time}, 类型: "number"

[打印], 内容: "=== 增强的默认值功能演示完成 ==="
[打印], 内容: "新增的默认值支持："
[打印], 内容: "- HTTP请求：客户端=default, 禁用授权=false, 重试次数=0, 重试间隔=1"
[打印], 内容: "- 断言：消息='断言失败'"
[打印], 内容: "- JSON断言：操作符='==', 消息='JSON断言失败'"
[打印], 内容: "- 类型断言：消息='类型断言失败'"
[打印], 内容: "- 数据比较：操作符='==', 消息='数据比较失败'"
[打印], 内容: "所有功能都工作正常！" 