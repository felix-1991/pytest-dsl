@name: 根目录测试前置
@description: 测试目录根级别的前置操作
@tags: [setup, root]

# 打印日志表明根目录setup执行了
[打印内容],内容: '=== 根目录(tests/)的setup.auto已执行 ==='

# 设置一些全局变量供子目录使用
g_root_base_url = 'https://api.example.com'
g_root_headers = '{"Content-Type": "application/json", "Accept": "application/json"}'
g_root_timeout = 30